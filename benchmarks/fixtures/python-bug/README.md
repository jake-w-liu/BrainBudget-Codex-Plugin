# Python Bug Fixture

Tiny repository for BrainBudget benchmark runs.

- `mathops.py` contains a deliberate bug
- `tests/` contains a small `unittest` suite
- `pyproject.toml` declares the project metadata
